name: CI

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  windows-build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.x'
          cache: true

      - name: Test
        run: go test ./...

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.15.0

      - name: Build
        run: wails build

      - name: Upload executable
        uses: actions/upload-artifact@v4
        with:
          name: ${{ startsWith(github.ref, 'refs/tags/v') && format('ime-lock-v2-{0}-windows-amd64', github.ref_name) || 'ime-lock-v2-windows-amd64' }}
          path: build/bin/ime-lock-v2.exe
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: windows-build
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - name: Download executable
        uses: actions/download-artifact@v4
        with:
          name: ime-lock-v2-${{ github.ref_name }}-windows-amd64
          path: dist

      - name: Rename release executable
        shell: pwsh
        run: Move-Item -Force dist\ime-lock-v2.exe dist\ime-lock-v2-${{ github.ref_name }}-windows-amd64.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: dist/ime-lock-v2-${{ github.ref_name }}-windows-amd64.exe
          generate_release_notes: true
